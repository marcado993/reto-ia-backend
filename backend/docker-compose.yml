services:
  api:
    build: .
    container_name: reto-ia-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: >
      sh -c "python scripts/seed_db.py && \
             uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
